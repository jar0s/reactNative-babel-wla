import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  display: {
    color: '#fff',
    fontSize: 40,
    textAlign: 'right',
    marginRight: 20,
    marginBottom: 10,
  },
});
